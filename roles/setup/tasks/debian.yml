---
- name: Install common packages [debian]
  become: true
  ansible.builtin.apt:
    update_cache: true
    pkg: "{{ setup_common_packages }}"
    state: present

- name: Install Debian-specific packages
  become: true
  ansible.builtin.apt:
    pkg: "{{ setup_debian_packages }}"
    state: present
