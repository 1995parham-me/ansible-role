---
- name: Set authorized keys taken from github
  ansible.posix.authorized_key:
    user: "{{ ansible_facts['user_id'] }}"
    state: present
    key: https://github.com/{{ setup_github_keys_username }}.keys
